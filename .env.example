# Safe template only. Copy to .env.local when Supabase is connected later.
# Put the real Supabase project URL and anon key in .env.local, never here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
